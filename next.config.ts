import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The floating dev badge overlaps the hero's scroll cue.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
